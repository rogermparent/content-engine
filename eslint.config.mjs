const config = {
  ignores: [
    "/node_modules",
    "/.pnp",
    ".pnp.js",
    ".yarn/install-state.gz",
    "/website/.next/",
    "/editor/.next/",

    "/website/out/",

    "/content",
    "/test-content",
  ],
};

export default config;
